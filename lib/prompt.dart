import 'package:flutter/material.dart';

class PromptManagementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('This is a prompt management page'),
      ),
    );
  }
}